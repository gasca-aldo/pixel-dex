import {hardwarePhotos} from '@/lib/hardware-variants';
export default function HardwareCredits(){return <main style={{maxWidth:760,margin:'40px auto',padding:24}}><a href="/">Back to My collection</a><h1>Hardware photo credits</h1><p>Photographs from Wikimedia Commons. Images are resized for display; product colors have not been changed by Pixel Dex.</p>{Object.entries(hardwarePhotos).map(([id,p])=><section key={id} style={{margin:'28px 0'}}><img src={p.src} alt={id.replaceAll('-',' ')} width={180} style={{maxHeight:150,objectFit:'contain'}}/><p><a href={p.source}>{id.replaceAll('-',' ')} — {p.author}</a></p><a href={p.licenseUrl}>{p.license}</a></section>)}</main>}
